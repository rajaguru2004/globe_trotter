# Docker environment variables
# Copy this file to .env.docker and customize for your deployment

# JWT Configuration
JWT_SECRET=GlobeTrotter2026ProductionSecretKeyPleaseChangeThis!@#$%
JWT_EXPIRES_IN=15m

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://localhost:8080

# Optional: Override PostgreSQL credentials (not recommended unless needed)
# POSTGRES_USER=travelplanar
# POSTGRES_PASSWORD=travelplanar_password
# POSTGRES_DB=travelplanar_db
